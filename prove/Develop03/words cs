using System.ComponentModel.Design.Serialization;
using System.Text.RegularExpressions;

namespace Develop03;

//keeps track of an individual word in a scripture.
public class words cs
{
    private string word;
    private bool IsInvisible = true;
    public word(string word)
    {
        this.word = word;
    }
    public void hide() (invisible = false;)

    public bool getIsVisible() =>isVisible

    public override string ToString()
    {
        return isVisible word : Regex.Replace(words, "[a-zA-Z]","_");
    }
}